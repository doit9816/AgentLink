{
  "name": "agentlink-desktop",
  "private": true,
  "version": "0.1.3",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev:ui": "vite --host 127.0.0.1 --port 1420 --strictPort",
    "build:ui": "vite build",
    "build:bundle-assets": "npm run build:ui && node ./scripts/stage-bundled-agentlink.mjs",
    "dev": "tauri dev",
    "build:tauri": "tauri build",
    "build:app": "tauri build --bundles app",
    "build:dmg": "./scripts/build-dmg-macos.sh",
    "build": "npm run build:tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-process": "^2.3.1",
    "@tauri-apps/plugin-updater": "^2.10.1",
    "vue": "^3.5.0",
    "vue-i18n": "^9.14.5"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@vitejs/plugin-vue": "^5.2.0",
    "vite": "^6.0.0"
  }
}
